This is a compiler for simple calculator which can have single letter variables [a-z] and supports functions as add, sub, mul, div, power. 

Operator precedence has been handled.

Enter exit or EXIT to end the calc.

Instructions for running the file's:

cd to the folder directory
run command : sudo chmod +x calc.sh
run command : ./calc.sh and calc will start.
